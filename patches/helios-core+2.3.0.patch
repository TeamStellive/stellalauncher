diff --git a/node_modules/helios-core/dist/common/distribution/DistributionFactory.js b/node_modules/helios-core/dist/common/distribution/DistributionFactory.js
index d2d927f..08d9cc1 100644
--- a/node_modules/helios-core/dist/common/distribution/DistributionFactory.js
+++ b/node_modules/helios-core/dist/common/distribution/DistributionFactory.js
@@ -188,7 +188,7 @@ class HeliosModule {
     resolveLocalPath(commonDir, instanceDir) {
         // Version Manifests have a pre-determined path.
         if (this.rawModule.type === helios_distribution_types_1.Type.VersionManifest) {
-            return (0, path_1.join)(commonDir, 'versions', this.rawModule.id, `${this.rawModule.id}.json`);
+            return this.sanitizeLocalPath((0, path_1.join)(commonDir, 'versions', this.rawModule.id, `${this.rawModule.id}.json`));
         }
         const relativePath = this.rawModule.artifact.path ?? MavenUtil_1.MavenUtil.mavenComponentsAsNormalizedPath(this.mavenComponents.group, this.mavenComponents.artifact, this.mavenComponents.version, this.mavenComponents.classifier, this.mavenComponents.extension);
         switch (this.rawModule.type) {
@@ -197,18 +197,29 @@ class HeliosModule {
             case helios_distribution_types_1.Type.ForgeHosted:
             case helios_distribution_types_1.Type.Fabric:
             case helios_distribution_types_1.Type.LiteLoader:
-                return (0, path_1.join)(commonDir, 'libraries', relativePath);
+                return this.sanitizeLocalPath((0, path_1.join)(commonDir, 'libraries', relativePath));
             case helios_distribution_types_1.Type.ForgeMod:
             case helios_distribution_types_1.Type.LiteMod:
                 // TODO Move to /mods/forge eventually..
-                return (0, path_1.join)(commonDir, 'modstore', relativePath);
+                return this.sanitizeLocalPath((0, path_1.join)(commonDir, 'modstore', relativePath));
             case helios_distribution_types_1.Type.FabricMod:
-                return (0, path_1.join)(commonDir, 'mods', 'fabric', relativePath);
+                return this.sanitizeLocalPath((0, path_1.join)(commonDir, 'mods', 'fabric', relativePath));
             case helios_distribution_types_1.Type.File:
             default:
-                return (0, path_1.join)(instanceDir, this.serverId, relativePath);
+                return this.sanitizeLocalPath((0, path_1.join)(instanceDir, this.serverId, relativePath));
         }
     }
+    sanitizeLocalPath(localPath) {
+        if (process.platform !== 'win32' || !localPath) {
+            return localPath;
+        }
+        const parsed = (0, path_1.parse)(localPath);
+        // 드라이브 문자 보존: C:\ 포함, 그 후 콜론만 변환
+        if (parsed.root) {
+            return parsed.root + localPath.slice(parsed.root.length).replace(/:/g, '_');
+        }
+        return localPath.replace(/:/g, '_');
+    }
     hasMavenComponents() {
         return this.mavenComponents != null;
     }
diff --git a/node_modules/helios-core/dist/dl/DownloadEngine.js b/node_modules/helios-core/dist/dl/DownloadEngine.js
index 922cd1a..71443ac 100644
--- a/node_modules/helios-core/dist/dl/DownloadEngine.js
+++ b/node_modules/helios-core/dist/dl/DownloadEngine.js
@@ -65,7 +65,8 @@ async function downloadQueue(assets, onProgress) {
     return receivedTotals;
 }
 async function downloadFile(url, path, onProgress) {
-    await (0, fs_extra_1.ensureDir)((0, path_1.dirname)(path));
+    const safePath = getFilesystemSafePath(path);
+    await (0, fs_extra_1.ensureDir)((0, path_1.dirname)(safePath));
     const MAX_RETRIES = 10;
     let fileWriterStream = null; // The write stream.
     let retryCount = 0; // The number of retries attempted.
@@ -83,7 +84,7 @@ async function downloadFile(url, path, onProgress) {
         }
         try {
             const downloadStream = got_1.default.stream(url);
-            fileWriterStream = (0, fs_1.createWriteStream)(path);
+            fileWriterStream = (0, fs_1.createWriteStream)(safePath);
             if (onProgress) {
                 downloadStream.on('downloadProgress', (progress) => onProgress(progress));
             }
@@ -119,6 +120,13 @@ async function downloadFile(url, path, onProgress) {
         throw error;
     }
 }
+function getFilesystemSafePath(path) {
+    if (process.platform !== 'win32' || !path) {
+        return path;
+    }
+    const parsed = (0, path_1.parse)(path);
+    return parsed.root ? parsed.root + path.slice(parsed.root.length).replace(/:/g, '_') : path.replace(/:/g, '_');
+}
 function retryableError(error) {
     if (error instanceof got_1.RequestError) {
         // error.name === 'RequestError' means server did not respond.
diff --git a/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js b/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js
index f09b6b4..7e1eed9 100644
--- a/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js
+++ b/node_modules/helios-core/dist/dl/distribution/DistributionIndexProcessor.js
@@ -76,7 +76,9 @@ class DistributionIndexProcessor extends IndexProcessor_1.IndexProcessor {
             return await this.loadVersionManifest(modLoaderModule);
         }
         else {
-            const zip = new node_stream_zip_1.default.async({ file: modLoaderModule.getPath() });
+            // 경로에서 콜론을 언더바로 변환 (Windows 파일 시스템 제약)
+            const safePath = this.sanitizePathForFS(modLoaderModule.getPath());
+            const zip = new node_stream_zip_1.default.async({ file: safePath });
             try {
                 const data = JSON.parse((await zip.entryData('version.json')).toString('utf8'));
                 const writePath = (0, FileUtils_1.getVersionJsonPath)(this.commonDir, data.id);
@@ -89,12 +91,24 @@ class DistributionIndexProcessor extends IndexProcessor_1.IndexProcessor {
             }
         }
     }
+    sanitizePathForFS(filePath) {
+        if (process.platform !== 'win32' || !filePath) {
+            return filePath;
+        }
+        const parsed = (0, path_1.parse)(filePath);
+        if (parsed.root) {
+            return parsed.root + filePath.slice(parsed.root.length).replace(/:/g, '_');
+        }
+        return filePath.replace(/:/g, '_');
+    }
     async loadVersionManifest(modLoaderModule) {
         const versionManifstModule = modLoaderModule.subModules.find(({ rawModule: { type } }) => type === helios_distribution_types_1.Type.VersionManifest);
         if (versionManifstModule == null) {
             throw new AssetGuardError_1.AssetGuardError('No mod loader version manifest module found!');
         }
-        return await (0, fs_extra_1.readJson)(versionManifstModule.getPath(), 'utf-8');
+        // 드라이브 문자 보존하면서 콜론 변환
+        const safePath = this.sanitizePathForFS(versionManifstModule.getPath());
+        return await (0, fs_extra_1.readJson)(safePath, 'utf-8');
     }
     // TODO Move this to a util maybe
     static isForgeGradle3(mcVersion, forgeVersion) {
diff --git a/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.js b/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.js
index 89fe157..8247234 100644
--- a/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.js
+++ b/node_modules/helios-core/dist/dl/receivers/FullRepairReceiver.js
@@ -95,7 +95,7 @@ class FullRepairReceiver {
         // Reduce load on IPC channel by sending only whole numbers.
         let currentPercent = 0;
         const receivedEach = await (0, DownloadEngine_1.downloadQueue)(this.assets, received => {
-            const nextPercent = Math.trunc((received / expectedTotalSize) * 100);
+            const nextPercent = expectedTotalSize > 0 ? Math.trunc((received / expectedTotalSize) * 100) : 0;
             if (currentPercent !== nextPercent) {
                 currentPercent = nextPercent;
                 process.send({ response: 'downloadProgress', percent: currentPercent });
diff --git a/node_modules/helios-core/dist/dl/receivers/ReceiverExecutor.js b/node_modules/helios-core/dist/dl/receivers/ReceiverExecutor.js
index 5f00414..fe3147e 100644
--- a/node_modules/helios-core/dist/dl/receivers/ReceiverExecutor.js
+++ b/node_modules/helios-core/dist/dl/receivers/ReceiverExecutor.js
@@ -39,7 +39,12 @@ process.on('message', async (message) => {
         // Our winston logger only outputs to stdout, so this works.
         // Write directly to stdout and await stdout flush.
         (0, fs_1.writeSync)(process.stdout.fd, 'Error now being propagated back to the transmitter.');
-        (0, fs_1.fsyncSync)(process.stdout.fd);
+        try {
+            (0, fs_1.fsyncSync)(process.stdout.fd);
+        }
+        catch (flushErr) {
+            log.debug('Unable to fsync stdout before propagating receiver error.', flushErr);
+        }
         process.send({
             response: 'error',
             displayable
